<% provide(:title, 'Edit Wad') %>

<div class="center-text">
  <div class="well well-modest left-text">
    <%= bootstrap_form_for(@wad) do |f| %>
      <legend>Edit Wad</legend>
      <%= f.text_field   :name %>
      <%= f.hidden_field :old_username, :value => @old_username %>
      <% if @wad.is_frozen? %>
        <%= f.text_field   :username, label: "/wads/***", disabled: true,
                           placeholder: "doom, chex, etc" %>
      <% else %>
        <%= f.text_field   :username, :label => "/wads/***",
                           :placeholder => "doom, chex, etc" %>
      <% end %>
      <%= f.text_field   :author %>
      <%= f.text_field   :iwad_username, :label => "Iwad",
                         :placeholer => "doom, chex, etc" %>
      <%= f.form_group :flags do %>
        <%= f.check_box :single_map, label: "Single Map?", checked: @wad.single_map %>
      <% end %>
      <%= f.submit "Update", class: "btn btn-primary pull-right" %>
    <% end %>
  </div>
</div>
