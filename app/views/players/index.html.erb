<% provide(:title, 'Players') %>
<div class="center-text">
  <div class="panel panel-default panel-modest left-text">
    <div class="panel-heading">
      <h3 class="panel-title">
        List of Players
        <% if logged_in? %>
          <%= link_to "Create New Player", new_player_path, :class => "label label-info pull-right" %>
        <% end %>
      </h3>
    </div>
    
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Demos</th>
          <th>Length</th>
        </tr>
      </thead>
      <tbody>
        <%= render @players %>
      </tbody>
    </table>
  </div>
</div>