<div class="navbar navbar-inverse">
  <div class="navbar-header">
    <a class="navbar-brand" href="#">WADs</a>
  </div>
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <% ( ('A'..'Z').to_a + ["#"] ).each do |char| %>
        <li class="wad-letter">
          <%= link_to char, "#" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>